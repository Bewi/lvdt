class Home extends Vue {
    constructor() {
        var properties = {
            el: '#home',
            data: {

            }
        }

        super(properties);
    }
};

export default Home;

